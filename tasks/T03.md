# T03 — Organizations + Users + Asset Types

> **Ler:** RULES.md + API_CONTRACTS.md (seções 4–5) + DATABASE.md
> **Depende de:** T02

## O que entregar

### 1. Organizations
- `internal/organization/repository.go` — FindByID, FindBySlug
- `internal/organization/service.go` — GetByID
- Migration seed com org default para dev

### 2. Users CRUD (ADMIN only)
- `internal/user/handler.go` — HandleList, HandleGet, HandleCreate, HandleUpdate, HandleDelete
- `internal/user/service.go` — List, GetByID, Create (hash senha), Update, SoftDelete
- `internal/user/repository.go` — List (filtros role/is_active + pagination), FindByID, Insert, Update, SoftDelete
- `internal/user/dto.go` — Create/Update requests + UserResponse

Regras: ADMIN only. Queries filtram por org_id do JWT. Email único por org.

### 3. Asset Types CRUD
- `internal/assettype/` — handler, service, repository, dto
- GET lista (todos podem), POST/PATCH (ADMIN only)
- Nome único por org.

### 4. Rotas
Registrar no router com middleware auth + RBAC.

## TDD — Obrigatório

> Siga Red → Green → Refactor. Escreva os testes **antes** do código.

**Formato de entrega:**
```
### Suposições
### Testes        ← código completo dos testes
### Implementação ← código completo
### Débitos Aceitos
```

## Aceite
- [x] ADMIN cria user → 201. TECH tenta → 403.
- [x] Email duplicado → 409
- [x] Delete faz soft delete
- [x] Asset types: ADMIN cria, TECH lista
- [x] Nome de tipo duplicado na org → 409
- [x] Tudo com paginação cursor-based
- [x] Audit log em cada operação de escrita